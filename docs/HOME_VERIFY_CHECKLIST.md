# 집 QA 스택

회사 PC에서 빌드 못 할 때, **집에서 `flutter run` 한 뒤 위에서 아래로** 훑는 확인 목록.  
작업이 쌓일수록 **맨 위에 레이어만 추가**하고, 아래 베이스는 그대로 둡니다.

---

## 오늘 세션

| 항목 | 값 |
|------|-----|
| 날짜 | |
| 브랜치 / 커밋 | |
| 기기 | |

```bash
cd chapter && flutter pub get && flutter run
```

온보딩 처음부터: 앱 데이터 삭제 또는 재설치.

---

## 스택 (위 → 아래, 위가 이번에 추가된 것)

> 새 작업 끝나면 **맨 위에 `## Layer N` 블록을 복사해서 추가**. 번호는 올려가며.

---

### Layer 5 — `2026-07-10` Phase B·C WYSIWYG + 되돌리기 플래그

- [ ] 기록 화면 **한 장** — PDF 레이아웃 위에서 무드·사진·글 직접 편집 (`kRecordBookPageComposer=true`)
- [ ] 무드: 헤더 「+ 무드」또는 칩 → 시트
- [ ] 사진: 페이지/칩 탭 → 추가·삭제·순서 → 배치 즉시 반영
- [ ] 글: 책 줄노트 박스에 직접 타이핑
- [ ] 저장 「한 장 더 쌓였어요 · 한 해 N%」+ 책등 연출 (`kRecordSaveAnimationV2`)
- [ ] 온보딩 2페이지 — PDF와 같은 페이지 비율
- [ ] **되돌리기:** `RECORD_BOOK_PAGE_COMPOSER=false` → Phase A 분리 UI 복원

---

### Layer 4 — `2026-07-10` Phase A · 기록 화면 책 페이지 미리보기

- [ ] *(Layer 5가 켜져 있으면 Layer 4는 `RECORD_BOOK_PAGE_COMPOSER=false` 로 따로 확인)*

---

### Layer 3 — `2026-07-07` 챕터 제거 · 온보딩 · 사진 수정 버그

- [ ] 온보딩 3단계: 소개 → 사진·일기 → 실물 책 → 시작하기
- [ ] 온보딩에 「완성 챕터」「Story Arc」 카피 **없음**
- [ ] 더보기에 「완성된 챕터」 **없음**, 통계에 「챕터 N개」 **없음**
- [ ] 저장 후 챕터 reveal / whisper 배너 **안 뜸**
- [ ] 내 책·월간 리포트 **기존처럼 동작**
- [ ] **사진 — 글만 수정 후 저장** → 사진 유지 (클라우드 URL만 있는 일기 포함)
- [ ] **사진 — 기존 사진 삭제** (스택 탭 → ✕, 순서 변경 목록 휴지통, 전체화면 삭제)
- [ ] **일기 — 이 날 기록 삭제** (보기 시트·수정 화면) → 확인 후 사진·글 모두 제거, 캘린더에서 빈 날
- [ ] **사진 — 추가·순서 변경** 후 저장 → 반영 유지
- [ ] 일기 저장 시 Story Arc AI 대기 **없음** (토큰 절약)

---

### Layer 2 — `2026-06` 월간 리포트 Phase 1

- [ ] 말일 이후 지난달 리포트 생성·reveal 1회
- [ ] 월간 상세에 팩트 digest (무드·장소·단어 등)
- [ ] 일기 수정 후 「다시 정리하기」만 (자동 덮어쓰기 없음)
- [ ] 캘린더 「이번 달」 버튼 (다른 달 볼 때만)

---

### Layer 1 — `2026-06` 강제 업데이트 · Remote Config

- [ ] `min_app_version` 미만이면 업데이트 차단 화면
- [ ] maintenance 모드 시 안내 화면
- [ ] 정상 버전이면 스플래시 → 홈

---

### Layer 0 — 베이스 (매 빌드마다)

- [ ] 앱 크래시 없이 홈 진입
- [ ] 오늘 일기: 사진 + 무드 + 저장 → 피드·캘린더에 표시
- [ ] 앱 재실행 후 오늘 기록·사진 유지
- [ ] 과거 날짜 기록 열기·수정·저장
- [ ] 더보기 → 내 책 화면 진입
- [ ] (선택) `flutter test test/entry_photos_test.dart`

---

## 새 레이어 추가 템플릿

맨 위(「스택」 제목 바로 아래)에 붙여넣기:

```markdown
### Layer N — `YYYY-MM-DD` 한 줄 요약

- [ ] 확인 항목 1
- [ ] 확인 항목 2
- [ ] 회귀: 기존에 깨지면 안 되는 것
```

---

## 이슈 로그 (계속 쌓기)

| 날짜 | Layer | 증상 | 재현 | 메모 |
|------|-------|------|------|------|
| | | | | |

---

## 자동 테스트 (레이어와 무관, 통과하면 기본 로직 OK)

```bash
flutter test test/entry_photos_test.dart
flutter test test/monthly_review_period_test.dart test/monthly_review_digest_test.dart
```
